# Core agent framework (pinned from successful build 2026-04-04)
deepagents==0.4.8
langgraph==1.0.10
langchain-core==1.2.26
langchain-anthropic==1.4.0
langchain-openai==1.1.12

# MCP integration (connects to Terraform MCP + AWS Docs MCP over stdio)
langchain-mcp-adapters==0.2.2

# CopilotKit AG-UI integration
copilotkit==0.1.84

# AgentCore Memory (checkpointer + store for LangGraph)
langgraph-checkpoint-aws==0.1.0

# AWS SDK (secret resolution at boot)
boto3==1.42.83

# GitHub PR submission
PyGithub==2.9.0

# YAML parsing
pyyaml==6.0.3

# OpenTelemetry tracing
opentelemetry-api==1.40.0
opentelemetry-sdk==1.40.0
opentelemetry-exporter-otlp-proto-grpc==1.40.0
opentelemetry-exporter-otlp-proto-http==1.40.0
opentelemetry-propagator-aws-xray==1.0.2
opentelemetry-sdk-extension-aws==2.1.0

# Testing (install with: pip install -e ".[test]")
pytest==8.4.2
hypothesis==6.151.10
