# Core agent framework
deepagents>=0.1.0
langgraph>=1.1.4,<2.0
langchain-core>=0.3.0,<1.0
langchain-anthropic>=0.3.0,<1.0
langchain-openai>=0.3.0,<1.0

# MCP integration (connects to Terraform MCP + AWS Docs MCP over stdio)
langchain-mcp-adapters>=0.1.0,<1.0

# CopilotKit AG-UI integration
copilotkit>=0.1.83,<1.0

# AgentCore Memory (checkpointer + store for LangGraph)
langgraph-checkpoint-aws>=0.1.0,<1.0

# AWS SDK (secret resolution at boot)
boto3>=1.35.0,<2.0

# GitHub PR submission
PyGithub>=2.5.0,<3.0

# YAML parsing
pyyaml>=6.0,<7.0

# OpenTelemetry tracing
opentelemetry-api>=1.28.0,<2.0
opentelemetry-sdk>=1.28.0,<2.0
opentelemetry-exporter-otlp-proto-grpc>=1.28.0,<2.0
opentelemetry-exporter-otlp-proto-http>=1.28.0,<2.0
opentelemetry-propagator-aws-xray>=1.0.0,<2.0
opentelemetry-sdk-extension-aws>=2.0.0,<3.0

# Testing (install with: pip install -e ".[test]")
pytest>=8.0,<9.0
hypothesis>=6.100,<7.0
