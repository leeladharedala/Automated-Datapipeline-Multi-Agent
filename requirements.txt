# Core agent framework
# deepagents async sub-agents are a PREVIEW API in the 0.6.x line (interface may change).
# 0.6.x adds the stream_events(version="v3") primitive + native reconnect/replay remote streaming.
# NOTE: 0.5.x -> 0.6.x is a breaking-surface upgrade; validate build() + memory integration (Req 16.4).
deepagents==0.6.8
langgraph>=1.2.8
langgraph-prebuilt>=1.0.8
langgraph-checkpoint>=3.0.1        # required by langgraph-api / langgraph-checkpoint-aws 1.x
langchain>=1.3.4                   # deepagents 0.6.8 requires >=1.3.4
langchain-core>=1.4.0              # deepagents 0.6.8 requires >=1.4.0
langchain-anthropic>=1.4.3         # deepagents 0.6.8 requires >=1.4.3
langchain-google-genai>=4.2.2      # newly mandatory transitive dep of deepagents 0.6.x
langchain-openai>=1.1.0
langchain-mcp-adapters>=0.2.0
# mcp 2.0 removed RequestContext from mcp.shared.context, but langchain-mcp-adapters
# 0.3.1 (latest) still imports it and only declares mcp>=1.24.0 — unpinned, pip picks
# 2.0.0 and `from langchain_mcp_adapters.client import MultiServerMCPClient` ImportErrors.
# Lift the cap only once langchain-mcp-adapters ships mcp 2.x support.
mcp>=1.24.0,<2.0.0
nest_asyncio>=1.6.0

# CopilotKit AG-UI integration
copilotkit>=0.1.94

# AgentCore Memory (checkpointer + store for LangGraph)
# Bumped to 1.x so langgraph-checkpoint>=3.0.1 (required by langgraph-api) resolves.
langgraph-checkpoint-aws>=1.2.0

# Co-located LangGraph Agent Protocol server + client SDK (async sub-agents)
# NOTE: langgraph-api 0.10.x is in Critical support (startup banner nags to
# upgrade), but pinning >=0.11 is ResolutionImpossible against
# aws-opentelemetry-distro==0.16.0 / langgraph-sdk — upgrading needs the OTel
# distro pin reworked first. The banner is advisory; 0.10.3 works.
langgraph-api>=0.10.1
langgraph-cli>=0.4.30
langgraph-sdk>=0.4.2

# AWS SDK
boto3>=1.42.0

# AgentCore Runtime SDK
bedrock-agentcore>=1.0.0

langchain-aws>=1.0.0

# Web server
fastapi>=0.115.0
uvicorn>=0.34.0

# GitHub PR submission
PyGithub>=2.9.0

# YAML parsing
pyyaml>=6.0

pandas>=2.2.0
pyarrow>=15.0.0

aws-opentelemetry-distro==0.16.0
opentelemetry-instrumentation-langchain
